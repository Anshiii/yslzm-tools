/// <reference types="react-scripts" />

type ShopItem = {
    name: string;
    count: number;
    position: string;
}


type LevelItem = {
    name: string, id: string, day: number, week: number, count?: number
}